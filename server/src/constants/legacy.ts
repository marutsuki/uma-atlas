// Legacy and Spark validation constants
export const SPARK_VALIDATION = {
    MIN_STARS: 1,
    MAX_STARS: 3,
} as const;

// Export for easy access
export const { MIN_STARS, MAX_STARS } = SPARK_VALIDATION;
